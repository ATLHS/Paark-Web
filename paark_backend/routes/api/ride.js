const router = require("express").Router();
const Ride = require("../../models/ride");

router.get("/user/:id/ride/:id", async (req, res) => {});
router.get("/user/:id/rides", async (req, res) => {});

module.exports = router;
